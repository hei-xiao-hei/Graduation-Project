using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tryonetry : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("成功的第一步，新建项目");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
